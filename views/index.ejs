<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CRUD App</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f7fc;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      width: 80%;
      max-width: 1200px;
      margin-top: 20px;
    }

    h1 {
      text-align: center;
      color: #333;
    }

    .card-container {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;
    }

    .card {
      background-color: white;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 300px;
      padding: 20px;
      text-align: center;
      transition: transform 0.3s;
    }

    .card:hover {
      transform: translateY(-10px);
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
    }

    .card h3 {
      margin: 0;
      color: #333;
      font-size: 1.5rem;
      margin-bottom: 10px;
    }

    .card p {
      color: #777;
      font-size: 1rem;
      margin-bottom: 20px;
    }

    .card a {
      text-decoration: none;
      margin: 10px 10px;
      padding: 8px 15px;
      font-size: 1rem;
      border-radius: 5px;
      transition: background-color 0.3s, color 0.3s;
    }

    .card .edit-btn {
      color: white;
      background-color: #007bff;
      border: 1px solid #007bff;
    }

    .card .edit-btn:hover {
      background-color: #0056b3;
      border-color: #0056b3;
    }

    .card .delete-btn {
      color: white;
      background-color: #dc3545;
      border: 1px solid #dc3545;
    }

    .card .delete-btn:hover {
      background-color: #c82333;
      border-color: #c82333;
    }

    .create-btn {
      display: block;
      margin: 20px auto;
      background-color: #28a745;
      color: white;
      padding: 10px 20px;
      text-align: center;
      text-decoration: none;
      border-radius: 5px;
      font-size: 1.2rem;
      transition: background-color 0.3s;
      max-width: fit-content;
    }

    .create-btn:hover {
      background-color: #218838;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>Items List</h1>
    <a href="/new" class="create-btn">Create New Item</a>
    <div class="card-container">
      <% items.forEach(item=> { %>
        <div class="card">
          <h3>
            <%= item.name %>
          </h3>
          <p>
            <%= item.description %>
          </p>
          <a href="/<%= item._id %>/edit" class="edit-btn">Edit</a>
          <a href="/<%= item._id %>/delete" class="delete-btn">Delete</a>
        </div>
        <% }) %>
    </div>
  </div>
</body>

</html>